using UnityEngine;
using System.Collections;

public class MusicScript : MonoBehaviour {

	// Use this for initialization
	void Start () {

		//just want to make sure the volume is very low on this one
		audio.volume = 0.1f;
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
